# An optional custom script to run before Hugo builds your site.
# You can delete it if you do not need it.

# blogdown::build_dir(here::here("content", "slide"))

# local({
#   owd <- setwd(here::here("content", "slide"))
#   on.exit(setwd(owd))
#
#   source("make.R")
# })
